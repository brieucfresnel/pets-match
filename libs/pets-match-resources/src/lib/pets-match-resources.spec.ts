import { petsMatchResources } from './pets-match-resources';

describe('petsMatchResources', () => {
  it('should work', () => {
    expect(petsMatchResources()).toEqual('pets-match-resources');
  });
});
